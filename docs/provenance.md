# Provenance Attestation Scaffold

Generate a provenance scaffold statement:

`./scripts/generate-provenance.sh`

Output:

- `dist/provenance/provenance.json`

## Purpose

- Capture commit and build-command context for release evidence.
- Serve as placeholder until signed SLSA/in-toto attestations are introduced.
